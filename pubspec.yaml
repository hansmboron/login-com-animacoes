name: login_animacoes
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_icons: ^1.0.0+1
  google_fonts: ^0.4.0
  flutter_mobx: ^1.1.0
  mobx: ^1.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  mobx_codegen:

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
   - img/
